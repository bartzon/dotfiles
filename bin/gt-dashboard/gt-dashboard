#!/usr/bin/env bash
# Graphite Dashboard launcher

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Run the dashboard
exec node "$SCRIPT_DIR/dist/index.js" "$@"